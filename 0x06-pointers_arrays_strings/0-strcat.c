#include "main.h"
#include <stdio.h>
#include <string.h>
/**
*_strcat - concat two string
*@dest: first input
*@src: second argu
*Return: array of string
*/
char *_strcat(char *dest, char *src)
{
strcat(dest, src);

return (dest);
}
