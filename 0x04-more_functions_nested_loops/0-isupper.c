#include "main.h"
/**
* _isupper - Checks for uppercase characters.*
*@c: is argument checked
*Return: 1 if is upper 0 otherwise
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
return (0);
}
