#include "main.h"
#include <stdio.h>
#include <ctype.h>
/*
 * Write a function that checks for lowercase character.
 */
int _islower(int c);
{
	int i;
 if (i > 96 && i < 123)
 {
  return 1;
 }
 else 
	 return 0;

}
